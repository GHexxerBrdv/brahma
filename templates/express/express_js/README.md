# {{project_name}}

A professional Express.js starter project with a modular architecture, created with **Brahma**.

## Getting Started

### 1. Prerequisites
- [Node.js](https://nodejs.org/) (v14 or higher)
- [MongoDB](https://www.mongodb.com/) (Local or Atlas)

### 2. Install Dependencies
```bash
npm install
```

### 3. Environment Setup
Create a `.env` file in the root directory and add your configuration:
```env
PORT=5000
MONGO_URI=your_mongodb_connection_string
```

### 4. Run the Application
```bash
# Development mode (with nodemon)
npm run dev

# Production mode
npm start
```

## Project Structure

```text
{{project_name}}/
├── src/
│   ├── server.js           # Entry point - starts the server
│   ├── app.js              # Express app configuration
│   ├── config/             # Database and other configurations
│   ├── controllers/        # Request handlers
│   ├── middlewares/        # Custom Express middlewares
│   ├── models/             # Database schemas (Mongoose)
│   ├── routes/             # API routes
│   ├── services/           # Business logic
│   └── utils/              # Utility functions and helpers
├── .env                    # Environment variables
├── .gitignore              # Git ignore rules
├── package.json            # Dependencies and scripts
└── README.md               # Project documentation
```

## Features
- **Modular Architecture**: Clean separation of concerns.
- **MongoDB Integration**: Pre-configured with Mongoose.
- **Error Handling**: Global error handling middleware.
- **Environment Support**: Easy configuration with `dotenv`.
- **CORS & Morgan**: Pre-installed for security and logging.

---
*Generated by [Brahma](https://github.com/GHexxerBrdv/brahma.git)*